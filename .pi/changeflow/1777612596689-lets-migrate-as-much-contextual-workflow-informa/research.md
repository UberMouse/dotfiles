# Research: lets migrate as much contextual workflow information out of the code and into defined meta properties on the workflow, ie things like "this state can be exited automatically" or "this state requires human approval", and they can be multiple things like "this state opens plannotator at the end" so you can combine to make "human approval with plan notator". With the meta params accepting arguments so you can specify ie that plannotator is launched to review a specific plan, or in diff mode etc

- 2026-05-01T05:18:18.921Z [note] Changeflow implementation is concentrated in pi/extensions/changeflow/index.ts with README.md documenting the lifecycle and Plannotator event API; npm run check currently passes from pi/extensions/changeflow.
- 2026-05-01T05:18:18.922Z [note] Current contextual workflow behavior is hard-coded in multiple helper functions: shouldAutoContinue enumerates agent-owned states; phasePrompt switch embeds per-state instructions and human-wait messages; tool_call allows source edits only in executing/qa; reviewStateForKind/reviewKindForState/submittedEventForKind map review kinds to states/events; submit tools have hard-coded allowed states.
- 2026-05-01T05:18:18.922Z [note] The XState machineDefinition currently stores only transitions. The persisted Workflow includes machineDefinition and machineSnapshot, so adding serializable state-level meta should flow into workflow.json for new workflows and can be read from the in-memory machine config/active state.
- 2026-05-01T05:18:18.923Z [note] XState v5 supports state node meta and tags. Local type declarations show StateNodeConfig has meta?: TMeta and tags?: SingleOrArray<TTag>; MachineSnapshot exposes tags, hasTag(tag), and getMeta(): Record<StateId, TMeta | undefined>. This can be used instead of separate state-name switch/sets.
- 2026-05-01T05:18:18.923Z [note] Plannotator integration is already abstracted behind requestReviewStatus(), requestPlanReview(), and REVIEW_RESULT_CHANNEL handling. requestPlanReview currently emits action: plan-review with payload { planContent, planFilePath, origin: changeflow } and creates ReviewRecord entries; README notes future code-review action for QA/code validation.
- 2026-05-01T05:18:26.672Z [note] A useful meta shape would be composable/argument-bearing, e.g. meta.changeflow = { autoContinue: true, editPolicy: 'artifactsOnly'|'sourceAllowed', prompt: { kind: 'research'|'planning'|... }, reviewGate: { actor: 'human', kind: 'high_level_plan'|'detailed_plan'|'code' }, plannotator: { action: 'plan-review'|'code-review', artifact: 'highLevelPlanPath'|'detailedPlanPath', mode?: 'diff' } }. Keeping it under a namespace avoids clashing with XState's own meta semantics.
- 2026-05-01T05:18:26.673Z [note] Potential refactor path: create helper getStateMeta(stateOrSnapshot) and small predicates/actions (stateAllowsSourceEdits, shouldAutoContinue, reviewKindForState, reviewStateForKind, promptKindForState) that read meta first. Then migrate existing hard-coded logic to consume meta without changing behavior.
- 2026-05-01T05:18:26.673Z [note] Existing workflows persist an older machineDefinition without meta. The code should tolerate missing meta and either fall back to the current canonical machineDefinition or use defaults, otherwise active/persisted workflows may lose behavior after the refactor.
- 2026-05-01T05:18:26.674Z [note] Validation available: cd pi/extensions/changeflow && npm run check. README contains manual Plannotator verification steps that should be updated after behavior moves to meta.
- 2026-05-01T05:18:35.250Z [note] Current state behavior mapping to preserve: auto-continue states are research, high_level_planning, high_level_revision, detailed_planning, execution_ordering, executing, qa. Source edits allowed only executing and qa. Human/Plannotator review gates are high_level_user_review=>high_level_plan and detailed_user_review=>detailed_plan. Plan submission transitions are high_level_plan=>HIGH_LEVEL_PLAN_SUBMITTED and detailed_plan=>DETAILED_PLAN_SUBMITTED.
- 2026-05-01T05:18:40.578Z [decision] Research complete: located hard-coded workflow context in Changeflow index.ts, confirmed XState meta/tags support, identified Plannotator integration points and backward-compatibility considerations.
- 2026-05-01T05:23:47.166Z [decision] Execution should proceed sequentially: first add metadata types and annotate the machine, then add metadata helpers, refactor behavior consumers, update docs, and finally run validation. These steps are tightly coupled in a single file and should be implemented in order.
- 2026-05-01T05:26:19.957Z [decision] Implemented metadata-driven Changeflow behavior: added typed state meta, annotated machineDefinition, refactored auto-continuation/edit policies/review mappings/submission states/prompt selection/Plannotator request arguments to read metadata, updated README, and validated with npm run check.
- 2026-05-01T05:29:14.618Z [decision] QA verified: TypeScript passes, no remaining hard-coded state-name checks for metadata-driven behavior, README updated with State metadata documentation, machineDefinition will be persisted with metadata on next workflow save.
